import { PageContainer } from "@/components/layout/PageContainer";
import TrustSignals from "@/components/sections/TrustSignals";

export default function TrustPage() {
  return (
    <PageContainer title="Novatra — Compliance & Trust Signals">
      <TrustSignals />
    </PageContainer>
  );
}
