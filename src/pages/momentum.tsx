import { PageContainer } from "@/components/layout/PageContainer";
import SocialProof from "@/components/sections/SocialProof";

export default function MomentumPage() {
  return (
    <PageContainer title="Novatra — Pilot Momentum & Proof">
      <SocialProof />
    </PageContainer>
  );
}
